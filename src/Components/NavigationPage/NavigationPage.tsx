import React from 'react';
import { IProps } from './types';
import styles from './NavigationPage.module.css';

const NavigationPage = (props: IProps) => {
  return <div className={styles.wrapper}>TODO</div>;
};

export default NavigationPage;
