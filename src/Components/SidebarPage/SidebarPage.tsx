import React from 'react';
import { IProps } from './types'
import styles from './SidebarPage.module.css';


const SidebarPage = (props : IProps) => {
  return (
    <div className={styles.wrapper}>TODO</div>
  );
};



export default SidebarPage;